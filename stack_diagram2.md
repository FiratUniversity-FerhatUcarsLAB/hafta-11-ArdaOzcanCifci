1. Başlangıç (main() metodu başlar)
  kod: zoop("just for", bizz);
  çıktı: (değişmedi)


2. zoop() metodu çağırıldı
  kod: System.out.println(fred);
  çıktı: just for

3. zoop() metodunun içindeki koşul sağlanır bob == 5 ping() metodu çağrılır
  kod: ping("not ");
  çıktı: (değişmedi)


4. ping() metodu çağrılır
  kod: System.out.println("any " + strangStrung + "more ");
  çıktı: just for
         any not more 

5. main() metodunun içindeki clink() metodu çağırılır
  kod: clink(2 * buzz)
  çıktı: (değişmedi)


6. clink metodu çağırılır
  kod: System.out.print("It's ");
  çıktı: just for 
         any not more
         It's


7. zoop() motodu ikinci kez çağırılır 
  kod: zoop("breakfast ", fork);
  çıktı (değişmedi)

  
8. zoop() metodu çalışır
  kod: System.out.println(fred);
  çıktı: just for
         any not more
         It's breakfast


9. zoop() metodudunun içindeki koşul sağlanma else içine gidilir
  kod: System.out.println("!");
  çıktı: just for
         any not more
         It's breakfast
         !
         











         
